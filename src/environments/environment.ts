export const environment = {
  production: false,
  // useMock: true will make the app use built-in mock services and seeded data
  // Set to false and provide a real environment.ts with Firebase config to use Firebase
  useMock: true,
  firebase: null as any
};
